# Instructions

> **TODO:** Fill in all `[Fill In]` placeholders before submitting.

This document explains how to set up your environment and run the project code.

---

## Requirements

- Python 3.x
- [Fill In: any other language/runtime requirements, e.g. Java, Spark, etc.]

### Python Packages

Install required packages with:

```bash
pip install -r requirements.txt
```

> **TODO:** Create a `requirements.txt` file listing your project's Python dependencies once they are known.

---

## Setup

1. **Clone this repository**

   ```bash
   git clone https://github.com/Hayden1629/bigdataproject.git
   cd bigdataproject
   ```

2. **Create and activate a virtual environment** (recommended)

   ```bash
   python -m venv venv
   source venv/bin/activate   # macOS/Linux
   venv\Scripts\activate      # Windows
   ```

3. **Install dependencies**

   ```bash
   pip install -r requirements.txt
   ```

4. **[Fill In: Any additional setup steps, e.g. configuring cloud credentials, downloading data, etc.]**

---

## Running the Analysis

### Jupyter Notebook

```bash
jupyter notebook notebooks/analysis.ipynb
```

### Python Script

```bash
python scripts/analysis.py
```

### [Fill In: Other commands, e.g. Spark submit, SQL scripts, etc.]

---

## Configuration

> **TODO:** Describe any configuration files or environment variables needed.

- Do **not** commit passwords, API keys, or credentials to this repository.  
  Use environment variables or a local `.env` file (add `.env` to `.gitignore`).

---

## Notes

[Fill In: Any additional notes or caveats for running the project.]
