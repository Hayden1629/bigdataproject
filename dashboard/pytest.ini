[pytest]
# Run from the dashboard/ directory so that bare imports (data_loader, analytics, …) resolve.
testpaths = tests
addopts   = -v --tb=short
# Marks
markers =
    unit:        Fast, pure-function unit tests (no I/O)
    integration: Tests that read parquet fixtures from disk
    smoke:       AppTest headless smoke tests (slower)
